version: '3.8'

services:
  backend:
    build: .
    container_name: facerecog_backend
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    environment:
      - PYTHONUNBUFFERED=1
